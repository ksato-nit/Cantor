#include "divisor.hpp"

Divisor::Divisor(){

}

Divisor Divisor::operator + (Divisor d2){
    Divisor d;

    return d;
}

Divisor Divisor::operator - (Divisor d2){
    Divisor d;

    return d;
}

Divisor inv(){
    Divisor d;

    return d;
}
